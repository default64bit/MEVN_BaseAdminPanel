<template>
    <teleport to="body">
        <transition name="fade" appear>
            <div class="t_dialog" :class="{ open: open }" v-if="open">
                <div class="backdrop" @click="close()"></div>
                <transition name="slidedown" appear>
                    <div class="box bg-gray-800" v-if="open">
                        <div class="flex justify-between items-center gap-4 mb-4">
                            <h2 class="text-lg">
                                <b>{{ title }}</b>
                            </h2>
                            <button class="t_button p-1 px-2 hover:bg-gray-600" @click="close()">
                                <i class="fas fa-times text-violet-400 text-lg"></i>
                            </button>
                        </div>
                        <slot name="body"></slot>
                    </div>
                </transition>
            </div>
        </transition>
    </teleport>
</template>

<script>
export default {
    name: "Dialog",
    props: ["open", "title"],
    data() {
        return {};
    },
    created() {},
    mounted() {},
    methods: {
        close() {
            this.$emit("update:open", false);
        },
    },
};
</script>

<style></style>
